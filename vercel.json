{
  "buildCommand": "cd evolvetech-crm && npm run build && cd .. && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd evolvetech-crm && npm install && cd .. && npm install",
  "rewrites": [
    {
      "source": "/crm/:path*",
      "destination": "/crm/index.html"
    }
  ]
}
